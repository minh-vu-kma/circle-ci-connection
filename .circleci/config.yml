version: 2.1

workflows:
  test-env-vars:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    environment:
      $CIRCLE_WORKFLOW_ID: << pipeline.id >>
    steps:
      - checkout
      - run:
          name: "echo an env var that is part of our project"
          command: |
            echo $CIRCLE_WORKFLOW_ID